<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sign In</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Arial', sans-serif;
        }

        body {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            background-color: #1E3A8A;
        }

        .signin-container {
            display: flex;
            background: white;
            border-radius: 10px;
            overflow: hidden;
            width: 800px;
            box-shadow: 0px 4px 20px rgba(0, 0, 0, 0.2);
        }

        .signin-image {
            background-color: #3B82F6;
            padding: 20px;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            color: white;
            flex: 1;
        }

        .signin-image img {
            width: 360px;
            margin-bottom: 20px;
        }

        .signin-image h2 {
            font-size: 24px;
            font-weight: bold;
        }

        .signin-form {
            flex: 1;
            padding: 40px;
            display: flex;
            flex-direction: column;
            justify-content: center;
        }

        .signin-form h1 {
            font-size: 24px;
            margin-bottom: 20px;
            color: #1E3A8A;
        }

        .signin-form form {
            display: flex;
            flex-direction: column;
        }

        .signin-form input {
            margin-bottom: 15px;
            padding: 10px;
            font-size: 16px;
            border: 1px solid #ccc;
            border-radius: 5px;
        }

        .signin-form button {
            padding: 10px;
            background: #1E3A8A;
            color: white;
            font-size: 16px;
            font-weight: bold;
            border: none;
            border-radius: 5px;
            cursor: pointer;
        }

        .signin-form button:hover {
            background: #3B82F6;
        }

        .signin-form a {
            color: #3B82F6;
            text-decoration: none;
        }

        .signin-form a:hover {
            text-decoration: underline;
        }

        .error-message {
            color: red;
            margin-bottom: 10px;
            font-size: 14px;
        }

        .signin-form .extra-links {
            display: flex;
            justify-content: space-between;
            margin-bottom: 10px;
        }

        .signin-form .extra-links a {
            font-size: 14px;
        }

        h3 {
            color:#1E3A8A;
        }

    </style>
</head>
<body>
    <div class="signin-container">
        <div class="signin-image">
            <img src="/images/2.png" alt="Paddles">
            <!-- <h2>Sign in to your account</h2> -->
        </div>
        <div class="signin-form">
            <h1>Sign In</h1>
            <% if (error) { %>
                <p class="error-message"><%= error %></p>
            <% } %>
            <form action="/api/employee/signin" method="POST">
                <input type="email" name="empEmail" placeholder="Email address" required>
                <input type="password" name="empPass" placeholder="Password" required>
                <div class="extra-links">
                    <h3> Don't have an account?</h3>
                    <a href="/signup">Sign up</a>
                </div>
                <button type="submit">Sign In</button>
            </form>
        </div>
    </div>
</body>
</html>
